nitialize npm

npm init -y
Step 3: Install Hardhat 2 specifically (not 3)

npm install --save-dev "hardhat@^2.22.0" "@nomicfoundation/hardhat-toolbox@^5.0.0"
The key difference is hardhat@^2.22.0 — this pins it to version 2 instead of letting it grab version 3.

Step 4: Initialize the Hardhat project again

npx hardhat init
This time when it asks, just select "Create a JavaScript project" and accept all defaults.

Run steps 1-3 first and let me know how it goes.